/*
Se desea un programa en Java que modele los vectores en un plano 3d. Por lo cual se solicita una clase Vector3D, que
incluya los atributos que usted considere necesarios y los siguientes métodos:

Constructor por defecto Debe asignar un estado inicial al objeto de 0 en sus coordenadas

Constructor sobrecargado con las tres coordenadas de un vector 3d, permite
inicializar las coordenadas del vector 3d con los valores de x, y, z recibidos como
parámetro.

Getter y Setter Getter y setter por cada atributo de la clase

getVector ()Debe retornar un String con los valores del vector, de la forma “(x, y, z)”, por
ejemplo: “(4, 6, 8)”

sumVector ()Debe aplicar la suma de vectores y retornar su resultado. Recibe como parámetros
las coordenadas x, y, z de un segundo vector.

moduleVector () Debe retornar el cálculo del módulo del vector

scalarMultiplication () Recibe como parámetro un valor entero y retorna el cálculo de su multiplicación por el vector.
 */
